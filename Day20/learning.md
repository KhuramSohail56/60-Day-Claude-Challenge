# Technical Key Learnings: Day 20

* **Boundary Math Sizing:** Mastered coordinate calculation by dividing mouse/touch tracking vector positions with calculated cell offsets to locate precise tile indices.
* **UI State Controls:** Resolved view visibility blocks by ensuring overlay modals explicitly toggle style mutations during data engine restarts.
* **Component Encapsulation:** Formulated clean, zero-dependency native DOM event streams that match both desktop cursor moves and mobile multi-touch events natively.
